import UIKit

final class TabBarController: UITabBarController {
    private enum Tab {
        case latest
        case browse
        case favorites
    }
    
    override func viewDidLoad() {
        super.viewDidLoad()

        
    }
    
}
